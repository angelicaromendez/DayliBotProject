services:
  - type: web
    name: dayli_bot_app
    env: docker
    plan: free
    repo: https://github.com/angelicaromendez/DayliBotProject
    branch: main
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dayli_bot_db
          property: connectionString
      - key: DJANGO_SUPERUSER_USERNAME
        value: "admin"  # Reemplaza con tu usuario admin
      - key: DJANGO_SUPERUSER_EMAIL
        value: "admin@example.com"  # Reemplaza con tu email
      - key: DJANGO_SUPERUSER_PASSWORD
        value: "adminpassword"  # Reemplaza con tu contraseña
    healthCheckPath: "/"
    autoDeploy: true

databases:
  - name: dayli_bot_db
    plan: free
    postgresVersion: 13
    region: oregon